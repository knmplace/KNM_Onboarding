# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files — NEVER commit these
.env
.env*.local
infra/.n8n.env

# PIN hash file — written by deploy.sh, never committed (contains bcrypt hash)
.pin-hash

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma (generated files)
prisma/migrations/
src/generated/

# logs
logs/
*.log
*.log.*

# local scratch/temp files
*.old
backups/

# MCP config (contains live API keys — never commit)
.mcp.json

# ADOB project planning files (local use only)
PLAN.md
TODO.md

# Internal tracking files — local only, never push
ERRORS.md
CHANGELOG.md
progress.md
PROJECT_PROGRESS.md

# Local SSH/dev notes — never commit (contains server IPs and auth hints)
.ssh.env
.claude-ssh.md
